---
import { getCollection } from 'astro:content';

const careers = await getCollection('careers');
const matches = careers.filter(p => 
  (p.slug + ' ' + (p.data.title || '')).toLowerCase().includes('engineer')
);
---

<html>
  <head>
    <title>Marine Engineer India Career Roadmap</title>
  </head>
  <body>
    <h1>Marine Engineer India Career Roadmap</h1>
    {matches.length ? (
      <ul>
        {matches.map(post => (
          <li><a href={`/careers/${post.slug}`}>{post.data.title || post.slug}</a></li>
        ))}
      </ul>
    ) : (
      <p>No engineering articles found.</p>
    )}
  </body>
</html>
