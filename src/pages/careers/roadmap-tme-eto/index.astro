---
import { getCollection } from 'astro:content';

const careers = await getCollection('careers');
const matches = careers.filter(p => 
  (p.slug + ' ' + (p.data.title || '')).toLowerCase().includes('tme') ||
  (p.slug + ' ' + (p.data.title || '')).toLowerCase().includes('eto') ||
  (p.slug + ' ' + (p.data.title || '')).toLowerCase().includes('engineer')
);
---

<html>
  <head>
    <title>TME/ETO Career Roadmap</title>
  </head>
  <body>
    <h1>TME/ETO Career Roadmap</h1>
    {matches.length ? (
      <ul>
        {matches.map(post => (
          <li><a href={`/careers/${post.slug}`}>{post.data.title || post.slug}</a></li>
        ))}
      </ul>
    ) : (
      <p>No TME/ETO articles found.</p>
    )}
  </body>
</html>
