---
import { getCollection } from 'astro:content';

const careers = await getCollection('careers');

// Group by subcategory
const grouped = careers.reduce((acc, post) => {
  const subcategory = post.data.subcategory || 'General';
  if (!acc[subcategory]) acc[subcategory] = [];
  acc[subcategory].push(post);
  return acc;
}, {} as Record<string, any[]>);
---

<html>
<head>
  <title>Career Guides - Sailor Success</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
</head>
<body>
  <h1>Career Guides</h1>
  
  {Object.entries(grouped).map(([subcategory, posts]) => (
    <section>
      <h2>{subcategory}</h2>
      <ul>
        {posts.map(post => (
          <li>
            <a href={`/careers/${post.slug}`}>{post.data.title}</a>
            {post.data.description && <p>{post.data.description}</p>}
          </li>
        ))}
      </ul>
    </section>
  ))}
</body>
</html>
