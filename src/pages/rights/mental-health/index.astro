---
import { getCollection } from 'astro:content';
const rights = await getCollection('rights');
const matches = rights.filter(p => (p.slug + ' ' + (p.data.title || '')).toLowerCase().includes('mental'));
---
<html><head><title>Mental Health at Sea</title></head><body><h1>Mental Health at Sea</h1>{matches.length ? <ul>{matches.map(p => <li><a href={`/rights/${p.slug}`}>{p.data.title || p.slug}</a></li>)}</ul> : <p>No guides found.</p>}</body></html>
