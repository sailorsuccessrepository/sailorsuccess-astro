---
import { getCollection } from 'astro:content';
const rights = await getCollection('rights');
const matches = rights.filter(p => (p.slug + ' ' + (p.data.title || '')).toLowerCase().includes('right') || (p.slug).includes('mlc'));
---
<html><head><title>Know Your Rights</title></head><body><h1>Know Your Rights</h1>{matches.length ? <ul>{matches.map(p => <li><a href={`/rights/${p.slug}`}>{p.data.title || p.slug}</a></li>)}</ul> : <p>No guides found.</p>}</body></html>
